services:
  mariadb:
    build: ./requirements/mariadb # Path to your custom mariadb Dockerfile
    image: mariadb
    container_name: mariadb
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - inception-net
    volumes:
      - mariadb_data:/var/lib/mysql # bind mount

  wordpress:
    build: ./requirements/wordpress # Path to your custom WP+PHP-FPM Dockerfile
    image: wordpress
    container_name: wordpress
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - inception-net
    depends_on:
      - mariadb
    volumes:
      - wordpress_data:/var/www/html # bind mount

  nginx:
    build: ./requirements/nginx # Path to your custom nginx Dockerfile
    image: nginx
    container_name: nginx
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - inception-net
    depends_on:
      - wordpress
    volumes:
      - wordpress_data:/var/www/html
    ports:
      - "443:443" # maps port 443 of the host to port 443 of the container

volumes:
  mariadb_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${HOME}/data/mariadb
  wordpress_data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: ${HOME}/data/wordpress

networks:
  inception-net:     
    driver: bridge